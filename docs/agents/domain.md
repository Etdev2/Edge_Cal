# Domain documentation

Edge Calculator uses a single-context domain layout.

Before working, agents should read:

- `CONTEXT.md` for canonical product terminology.
- Relevant records under `docs/adr/` for architectural decisions.

Create these documents only when terminology or a meaningful architectural
decision has actually been resolved. Use glossary terms consistently and flag
conflicts with existing decisions.
